pipeline {
  agent any
  stages {
    stage('') {
      steps {
        build 'Check'
        echo 'Hello'
        sh 'pwd'
      }
    }

  }
  environment {
    CredentialsID = 'ManojReddy-Bitbucket'
  }
}